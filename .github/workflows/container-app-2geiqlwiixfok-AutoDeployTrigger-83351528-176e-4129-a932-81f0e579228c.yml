name: Trigger auto deployment for container-app-2geiqlwiixfok

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-2geiqlwiixfok-AutoDeployTrigger-83351528-176e-4129-a932-81f0e579228c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP2GEIQLWIIXFOK_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP2GEIQLWIIXFOK_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP2GEIQLWIIXFOK_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP2GEIQLWIIXFOK_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP2GEIQLWIIXFOK_REGISTRY_PASSWORD }}
          containerAppName: container-app-2geiqlwiixfok
          resourceGroup: streamlitpython9e32
          imageToBuild: default/container-app-2geiqlwiixfok:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


